// Shape of the settings Marshal reads from chrome.storage.local.
// Don't put real keys here. Paste them into the extension's options page instead.
export const exampleConfig = {
  ASSEMBLYAI_API_KEY: "your-assemblyai-key",
  OPENAI_API_KEY: "sk-...",
};
